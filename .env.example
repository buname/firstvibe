# Production obfuscation is on by default when NODE_ENV=production.
# Set to "false" to disable during troubleshooting.
NEXT_ENABLE_OBFUSCATION=true

# Comma-separated list of extra trusted HTTPS origins for API access.
# The app's own origin is always allowed automatically.
ALLOWED_ORIGINS=https://example.com,https://app.example.com
